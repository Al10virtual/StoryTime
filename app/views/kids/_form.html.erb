<%= simple_form_for [family, kid] do |f| %>
  <%= f.input :first_name, label: "Prénom", placeholder: 'Nom de votre enfant' %>
  <%= f.input :date_of_birth, label: "Date de naissance", input_html: { data: { controller: "datepicker" } } %>
  <%= f.input :photo, as: :file, label: "Photo de l'enfant", accept: 'image/png,image/jpeg' %>
  <%= f.button :submit, class: "btn btn-flat w-100", value: "Ajouter mon enfant" %>
<% end %>
